<div class="card">
  <% @posts.each do |post| %>
    <h2><%=post.title%></h2>
    <h5><%=post.created_at%></h5>
    <div class="fakeimg" style="height:200px;">Image</div>
    <p><%=post.body%></p>
  <% end %>
</div>
