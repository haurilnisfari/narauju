<%= form_for(@post) do |f| %>
  <%= f.label :title %></br>
  <%= f.text_field :title %></br>

  <%= f.label :body %></br>
  <%= f.rich_text_area :body %></br>

  <%= f.label :categories %></br>
  <% @categories.each do |category| %>
    <%= f.check_box :categories, { multiple: true }, category.id, false %> <%= category.name %> </br>
  <% end %>
  <%= f.submit "Save" %></br>
<%end%>
